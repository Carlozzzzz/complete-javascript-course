// Number = floating numbers  and integers
let age = 23;

// String = sequence of characters - used for text 
let firstName = 'Carlos';

// Boolean = logical type that can only be true or false - used for taking decisions
let fullAge = true;

// Undefined = value take by a variable that is not yet defined - empty values
let children;

// Null = means 'empty value'

// Symbol (ES2025) = value that is unique and cannot be changed

// Bigint = larger integers than the Numbers type can hold

// dynamic typing = we do not have to manually define the data type of the value stored in variable. automatically

/**
 * multi comment
 */